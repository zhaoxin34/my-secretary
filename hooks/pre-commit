#!/bin/bash
# Pre-commit hook: run pytest and ruff check

set -e

echo ">>> Running pre-commit checks..."

# Run ruff check
echo ">>> Running ruff check..."
uv run ruff check --fix src/ tests/

# Run pytest
echo ">>> Running pytest..."
PYTHONPATH=src uv run pytest

echo ">>> All checks passed!"
